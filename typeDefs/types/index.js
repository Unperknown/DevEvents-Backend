const { eventType } = require('./eventType')
const { crawledType } = require('./crawledType')
const { requestorType } = require('./requestorType')

module.exports = {
  eventType,
  crawledType,
  requestorType,
}